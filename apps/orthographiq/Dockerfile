# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build orthographiq`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t orthographiq`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system orthographiq && \
          adduser --system -G orthographiq orthographiq

COPY dist/apps/orthographiq orthographiq
RUN chown -R orthographiq:orthographiq .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix orthographiq --omit=dev -f install

CMD [ "node", "orthographiq" ]
